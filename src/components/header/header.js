import React, { PropTypes, Component } from 'react'
import styles from './header.scss'

class Header extends Component {
	render () {
		return (
			<section className="header">
				<h1>Header mein tail rammstein</h1>
			</section>
		)
	}
}

export default Header